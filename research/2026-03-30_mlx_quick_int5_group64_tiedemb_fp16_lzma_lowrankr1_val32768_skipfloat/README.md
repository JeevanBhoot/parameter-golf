# Low-Rank Residual Breadth-First Scaffold

This scaffold carries the shared quick-track script branch for low-rank residual reconstruction experiments. It adds an architecture-agnostic low-rank correction on top of grouped low-bit quantization, with an optional activation-shaped variant that reuses deterministic calibration statistics.
