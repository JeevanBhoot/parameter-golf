- hypothesis: A role-level mixed-bit rule may recover much of the `INT6` quality gain without giving up the byte efficiency of the `INT5 + group64 + fp16 tied head` baseline. If we automatically upgrade only the single most error-sensitive tensor role across layers, we should get a better quality/size tradeoff than blanket `INT6`.
- exact change made: Starting from `INT5 + group64 + fp16 tied head`, score quantizable tensor roles by their mean relative reconstruction MSE under `INT5` after stripping the block index from names, upgrade the top-scoring role family to `INT6` across all layers, and leave all other quantized tensors at `INT5`.
- why this is in scope: This is an architecture-agnostic mixed-precision quantization rule. It avoids hand-picked layer exceptions and instead uses a reproducible sensitivity heuristic that can transfer across models with repeated block structure.
- expected effect on val_bpb: Improve relative to plain `INT5 + group64 + fp16 tied head`, ideally moving toward the `INT6` quality point without needing full `INT6` everywhere.
- expected effect on artifact bytes: Increase modestly over plain `INT5 + group64 + fp16 tied head`, while staying well below blanket `INT6 + group64 + fp16 tied head`.
- train memory budget rule: Keep `train_peak_rss_bytes` at or near the quick baseline.
- final result: `val_bpb=1.91639091`, `artifact_bytes=8227093`, `train_peak_rss_bytes=1534689280`, `time=16:43.52 total`, `auto_high_precision_roles=mlp.proj.weight`.
- short conclusion: Discard for now. The automatic role-ranking heuristic behaved as intended and chose `mlp.proj.weight` across layers, but this rerun stopped earlier than the stronger `INT5`/`INT6` references and finished with a worse exact `val_bpb` despite the larger artifact, so the current top-1 role-upgrade rule does not justify its byte cost.
